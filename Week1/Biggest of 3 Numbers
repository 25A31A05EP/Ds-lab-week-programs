#include <iostream>
using namespace std;

int main() {
    int a,b,c;

    cout<<"Enter three numbers: ";
    cin>>a>>b>>c;

    if(a>b && a>c)
        cout<<"Biggest = "<<a;
    else if(b>c)
        cout<<"Biggest = "<<b;
    else
        cout<<"Biggest = "<<c;

    return 0;
}
